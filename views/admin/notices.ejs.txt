<!DOCTYPE html>
<html lang="en">
<head><meta charset="UTF-8"><title>Notices Manager</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="bg-light">
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <h2 class="fw-bold text-success m-0">Notices Manager</h2>
        <a href="/admin/dashboard" class="btn btn-secondary btn-sm">Back to Dashboard</a>
    </div>

    <div class="card p-4 border-0 shadow-sm mb-4 bg-white">
        <h5 class="fw-bold text-success border-bottom pb-2 mb-3">Add New Public Notice</h5>
        <form action="/admin/notices/add" method="POST" class="row g-3">
            <div class="col-md-12">
                <label class="form-label fw-semibold">Notice Title</label>
                <input type="text" name="title" class="form-control" required placeholder="Ex: Admission Open For New Session">
            </div>
            <div class="col-md-12">
                <label class="form-label fw-semibold">Content Text Message</label>
                <textarea name="content" class="form-control" rows="3" required placeholder="Write details information cleanly..."></textarea>
            </div>
            <div class="col-12 text-end">
                <button type="submit" class="btn btn-success">Publish Announcement</button>
            </div>
        </form>
    </div>

    <h4 class="fw-bold text-dark mb-3">Active Board Publications</h4>
    <div class="row g-3">
        <% notices.forEach(notice => { %>
            <div class="col-12">
                <div class="card p-3 border-0 shadow-sm bg-white">
                    <form action="/admin/notices/edit" method="POST" class="row g-2 align-items-end">
                        <input type="hidden" name="id" value="<%= notice.id %>">
                        <div class="col-md-4">
                            <label class="small fw-semibold text-muted">Title Block</label>
                            <input type="text" name="title" class="form-control form-control-sm fw-bold text-success" value="<%= notice.title %>" required>
                        </div>
                        <div class="col-md-5">
                            <label class="small fw-semibold text-muted">Details Description</label>
                            <textarea name="content" class="form-control form-control-sm" rows="1" required><%= notice.content %></textarea>
                        </div>
                        <div class="col-md-3 text-end">
                            <button type="submit" class="btn btn-sm btn-primary me-1">Update</button>
                            <button type="submit" formAction="/admin/notices/delete" class="btn btn-sm btn-danger" onclick="return confirm('Wipe announcement data permanently?')">Delete</button>
                        </div>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</div>
</body>
</html>