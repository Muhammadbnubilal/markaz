<!DOCTYPE html>
<html lang="en">
<head><meta charset="UTF-8"><title>Upcoming Events</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="bg-light">
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <h2 class="fw-bold text-success m-0">Upcoming Events Management</h2>
        <a href="/admin/dashboard" class="btn btn-secondary btn-sm">Back to Dashboard</a>
    </div>

    <div class="card p-4 border-0 shadow-sm mb-4 bg-white">
        <h5 class="fw-bold text-success border-bottom pb-2 mb-3">Add New Upcoming Event</h5>
        <form action="/admin/events/add" method="POST" class="row g-3">
            <div class="col-md-8">
                <label class="form-label fw-semibold">Event Name / Event Title</label>
                <input type="text" name="title" class="form-control" required placeholder="Ex: Islamic Knowledge Contest">
            </div>
            <div class="col-md-4">
                <label class="form-label fw-semibold">Event Date</label>
                <input type="date" name="event_date" class="form-control" required>
            </div>
            <div class="col-md-12">
                <label class="form-label fw-semibold">Brief Description</label>
                <textarea name="description" class="form-control" rows="2" placeholder="Time details or location descriptions inside campus..."></textarea>
            </div>
            <div class="col-12 text-end">
                <button type="submit" class="btn btn-success">Save Upcoming Event</button>
            </div>
        </form>
    </div>

    <h4 class="fw-bold text-dark mb-3">Published Schedule Items</h4>
    <div class="row g-3">
        <% events.forEach(event => { %>
            <div class="col-12">
                <div class="card p-3 border-0 shadow-sm bg-white">
                    <form action="/admin/events/edit" method="POST" class="row g-2 align-items-end">
                        <input type="hidden" name="id" value="<%= event.id %>">
                        <div class="col-md-3">
                            <label class="small fw-semibold text-muted">Event Name</label>
                            <input type="text" name="title" class="form-control form-control-sm fw-bold text-success" value="<%= event.title %>" required>
                        </div>
                        <div class="col-md-2">
                            <label class="small fw-semibold text-muted">Date Schedule</label>
                            <input type="date" name="event_date" class="form-control form-control-sm text-warning font-monospace" value="<%= new Date(event.event_date).toISOString().split('T')[0] %>" required>
                        </div>
                        <div class="col-md-4">
                            <label class="small fw-semibold text-muted">Description Text</label>
                            <input type="text" name="description" class="form-control form-control-sm text-muted" value="<%= event.description %>">
                        </div>
                        <div class="col-md-3 text-end">
                            <button type="submit" class="btn btn-sm btn-primary me-1">Update</button>
                            <button type="submit" formAction="/admin/events/delete" class="btn btn-sm btn-danger" onclick="return confirm('Wipe event catalog item entry?')">Delete</button>
                        </div>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</div>
</body>
</html>