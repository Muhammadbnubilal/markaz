<!DOCTYPE html>
<html lang="en">
<head><meta charset="UTF-8"><title>Inbox Contact Messages</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="bg-light">
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <h2 class="fw-bold text-success m-0">Inbox Contact Form Messages</h2>
        <a href="/admin/dashboard" class="btn btn-secondary btn-sm">Back to Dashboard</a>
    </div>

    <div class="row g-3">
        <% if (messages && messages.length > 0) { %>
            <% messages.forEach(msg => { %>
                <div class="col-12">
                    <div class="card p-3 border-0 shadow-sm bg-white">
                        <div class="d-flex justify-content-between border-bottom pb-2 mb-2">
                            <span class="fw-bold text-success fs-5"><%= msg.name %></span>
                            <span class="text-muted small font-monospace"><%= new Date(msg.created_at).toLocaleString() %></span>
                        </div>
                        <p class="mb-2 bg-light p-2 rounded text-dark fs-6">"<%= msg.message %>"</p>
                        <div class="small">
                            <span class="me-3"><strong>Phone Contact:</strong> <%= msg.phone %></span>
                            <span><strong>Email Box:</strong> <a href="mailto:<%= msg.email %>" class="text-decoration-none"><%= msg.email %></a></span>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="text-center text-muted py-5 col-12">The inbox message log database is completely empty.</div>
        <% } %>
    </div>
</div>
</body>
</html>s